let a = [20, 40, 50, 60, 30];
console.log(a.length);

// predefine reverse method =====>>
console.log(a.reverse());

// for loop method ======>>
for (let i = a.length - 1; i >= 0; i--) {
  console.log(a[i]);
}
